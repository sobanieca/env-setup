#!/bin/bash

# Read the API key from the secrets file
apiKey=$(cat ~/.secrets/anthropic.txt)

# Launch aider with the API key
aider --api-key anthropic="$apiKey" --model anthropic.claude-3-5-sonnet-20241022-v2:0 "$@"

