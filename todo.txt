timezone setting - sudo dpkg-reconfigure tzdata
tmux git plugin (https://github.com/drmad/tmux-git)
git config: 
    git config --global core.editor "vim"
    git config --global user.name "abc"
    git config --global user.email "abc"
set hostname
sudo update-alternatives --config editor (setting default text editor)
tmux resurrect
let g:NERDTreeWinSize=60 in .vimrc
nmap ,n :NERDTreeFind<CR>
https://unix.stackexchange.com/questions/141097/how-to-enable-and-use-code-folding-in-vim
let g:ctrlp_custom_ignore = 'node_modules\|DS_Store\|git' in .vimrc
let g:tsuquyomi_use_local_typescript = 0
tmux.conf (bind-key -r < swap-window -t -1; bind-key -r > swap-window -t +1)
"apt-get install build-essential"
