move root specific operations from ssh-config.sh to root-ssh-config.sh
apt-get install curl
timezone setting - sudo dpkg-reconfigure tzdata
tmux git plugin (https://github.com/drmad/tmux-git)
set encoding=utf-8  to vimrc
set nowrap - disable wrapping text (to vimrc)
alias grep="grep --color=auto" (to bashrc)
git config: 
    git config --global core.editor "vim"
    git config --global user.name "abc"
    git config --global user.email "abc"
sudo update-alternatives --config editor (setting default text editor)
tmux resurrect
git log alias
    alias glog="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
vim colors

