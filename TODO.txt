fix creating folders/files - when running as sudo -E owner is root, it should be current user that runs the script
timezone setting - sudo dpkg-reconfigure tzdata
tmux git plugin (https://github.com/drmad/tmux-git)
git config: 
    git config --global core.editor "vim"
    git config --global user.name "abc"
    git config --global user.email "abc"
sudo update-alternatives --config editor (setting default text editor)
tmux resurrect
install script - sudo apt-get install curl, download from github raw files
